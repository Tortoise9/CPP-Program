#include<iostream> //header file for the input output funtions
int main(){
    std::cout<<"hello world "; //cout is the object of ostream (i.e class) or output statement 
    // cout is define in the std namespace . (it call standard library where cout is present )
    return 0;
}